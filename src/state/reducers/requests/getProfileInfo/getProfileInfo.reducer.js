import { createRequestResponseReducer } from '../../generic';
import { GetProfileInfoRequestTypes } from '../../../types/requests/getProfileInfo/getProfileInfo.types';

export default createRequestResponseReducer(GetProfileInfoRequestTypes);
