import { createRequestResponseReducer } from '../../generic';
import { LoginUserRequestTypes } from '../../../types/requests/loginUser/loginUser.types';

export default createRequestResponseReducer(LoginUserRequestTypes);
